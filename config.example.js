// Copy this file to "config.js" and fill in your real invite code(s).
// config.js is listed in .gitignore, so it will never be committed to GitHub.
window.QUIZ_CONFIG = {
  inviteCodes: ['YOUR-CODE-HERE'],
};
